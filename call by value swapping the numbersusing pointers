#include<stdio.h>
int swap(int ,int);
int main( )
{
    int a,b;
    printf("Enter two numbers: ");
    scanf("%d %d",&a,&b);
    swap(a,b);
    printf("%d %d",a,b);
    return 0;
}
int swap( int, int)
{
    int x,y,t;
    t=x;
    x=y;
    y=t;
}
